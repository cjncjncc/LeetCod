public class ZigZagConversion {
	  public String convert(String s, int nRows) {
		 int Seg;
		 int SegLength;
		 int level;
		 SegLength=nRows+nRows/2;
		 Seg=s.length()/SegLength;
		
		return s;		
	 }
}
